name: Platform

on: # yamllint disable-line rule:truthy
  pull_request:
    types:
      - opened
      - edited

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  bookkeeping:
    name: Bookkeeping
    uses: theholocron/.github/.github/workflows/bookkeeping.yml@main
    secrets: inherit
