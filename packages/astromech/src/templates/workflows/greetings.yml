name: Platform

on: # yamllint disable-line rule:truthy
  pull_request:
  issues:

permissions:
  issues: write
  pull-requests: write

jobs:
  greetings:
    name: Greetings
    uses: theholocron/.github/.github/workflows/greetings.yml@main
    secrets: inherit
