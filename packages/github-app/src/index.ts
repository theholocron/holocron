/**
 * `@theholocron/github-app` — Holocron's minimal GitHub App.
 *
 * Webhook receiver, default-branch-only `holocron.config.ts` validation,
 * custom-properties sync, one check run per resolution run. Design:
 * `.notes/tech-github-app-v1.spec.md` (repo root).
 *
 * Scaffolding only — the webhook receiver, schema validation, and
 * check-run posting land in follow-up PRs once the deploy target is
 * decided (see the spec's "Open, not yet decided" section).
 */

export {};
