# AUTO-GENERATED — do not edit directly.
# Source:  theholocron/holocron · packages/cli/src/commands/setup-workflows.ts
# Synced:  2026-08-31T00:00:00.000Z
# Tool:    holocron setup
# Changes: run `holocron setup` to regenerate.
name: ADR & spec check

on: # yamllint disable-line rule:truthy
  push:
    branches: [main, alpha]
    paths:
      - "docs/architecture/adr/**"
      - ".notes/**"
  pull_request:
    paths:
      - "docs/architecture/adr/**"
      - ".notes/**"

concurrency:
  group: adr-check-${{ github.ref }}
  cancel-in-progress: true

jobs:
  adr-check:
    uses: theholocron/.github/.github/workflows/adr-check.yml@main
    secrets: inherit
