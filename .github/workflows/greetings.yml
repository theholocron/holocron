name: Greetings

on: # yamllint disable-line rule:truthy
  pull_request:
  issues:

permissions:
  issues: write
  pull-requests: write

jobs:
  greetings:
    uses: theholocron/.github/.github/workflows/ci-greetings.yml@main
    secrets: inherit
