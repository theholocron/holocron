name: Dependencies

on: # yamllint disable-line rule:truthy
  pull_request:

permissions:
  contents: write
  pull-requests: write

jobs:
  dependencies:
    uses: theholocron/.github/.github/workflows/ci-dependencies.yml@main
    secrets: inherit
