name: Stale

on:
  schedule:
    - cron: "30 1 * * *"

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  stale:
    uses: theholocron/.github/.github/workflows/stale.yml@main
    secrets: inherit
