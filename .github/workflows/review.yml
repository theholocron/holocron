name: Review

on: # yamllint disable-line rule:truthy
  pull_request:

permissions:
  contents: read
  checks: write
  pull-requests: write

jobs:
  review:
    uses: theholocron/.github/.github/workflows/review.yml@main
    secrets: inherit
